import React from "react";

export const RadiusCounter=({radius,bgColor})=>{
    return(
        <div className="sub" style={{borderRadius:radius, backgroundColor:bgColor}}>
            <h1>Assignment-10 </h1>
           
        </div>

    )
}